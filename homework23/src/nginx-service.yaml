apiVersion: v1
kind: Service
metadata:
  name: nginx-service-allex-devops
spec:
  type: NodePort
  selector:
    app: nginx-allex-devops
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007 