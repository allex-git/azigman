apiVersion: v1
kind: Service
metadata:
  name: hw24-python-random-service
spec:
  selector:
    app: hw24-python-random
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
  type: NodePort